# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# Variables de entorno (aquí irá la API key si añades alguna en el futuro)
.env

# Base de datos — se genera con seed.py, no se versiona*
# * Excepción: cuando vayas a desplegar en Render la primera vez,
#   comenta esta línea temporalmente, sube la BD, y vuelve a activarla.
backend/crestle.db

# Escudos — igual, generados por seed.py
# Misma excepción que la BD para el primer despliegue.
backend/crests/

# Editores y OS
.DS_Store
.vscode/
*.swp